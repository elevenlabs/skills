{
  "skill_name": "setup-api-key",
  "evals": [
    {
      "id": 1,
      "prompt": "Help me set up my ElevenLabs API key. I just created an account but don't know where to find the key or how to configure it.",
      "expected_output": "Step-by-step guide to finding and configuring the ElevenLabs API key without exposing the key in chat",
      "files": [],
      "expectations": [
        "Checks whether ELEVENLABS_API_KEY is already set in the environment or .env",
        "Provides instructions to find the API key in the ElevenLabs dashboard",
        "Tells the user to add ELEVENLABS_API_KEY to a local .env file",
        "Explicitly tells the user not to paste the API key into chat",
        "Validates the locally configured key by making a test API call to the user endpoint"
      ]
    },
    {
      "id": 2,
      "prompt": "I'm getting 401 errors when calling the ElevenLabs API. My code was working yesterday but now it's broken. Help me debug this.",
      "expected_output": "Debugging steps for API key issues including local configuration validation",
      "files": [],
      "expectations": [
        "Checks the current ELEVENLABS_API_KEY environment variable or .env value without printing it",
        "Attempts to validate the configured key against the ElevenLabs API",
        "Suggests common causes (expired key, wrong key, environment not loaded)",
        "Provides steps to get a new key and save it in .env if the current one is invalid",
        "Does not ask the user to paste the API key into chat"
      ]
    }
  ]
}
